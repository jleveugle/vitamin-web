import Button from './Button.svelte';

export default {
  title: 'Example/VtmnButton',
  component: Button,
};

const Template = () => ({
  Component: Button,
});

export const Primary = Template.bind({});
Primary.args = {};
